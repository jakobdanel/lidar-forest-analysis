% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kld.R
\name{lfa_kld}
\alias{lfa_kld}
\title{Kullback-Leibler Divergence Calculation}
\usage{
lfa_kld(p, q, epsilon = 1e-10)
}
\arguments{
\item{p}{A numeric vector representing the probability distribution P.}

\item{q}{A numeric vector representing the probability distribution Q.}

\item{epsilon}{A small positive constant added to both P and Q to avoid logarithm of zero. Default is 1e-10.}
}
\value{
A numeric value representing the Kullback-Leibler Divergence between P and Q.
}
\description{
This function calculates the Kullback-Leibler Divergence (KLD) between two probability distributions P and Q.
}
\details{
The KLD is computed using the formula:
\code{sum(p * log((p + epsilon) / (q + epsilon)))}
This avoids issues when the denominator (Q) contains zero probabilities.
}
\examples{
# Calculate KLD between two probability distributions
p_distribution <- c(0.2, 0.3, 0.5)
q_distribution <- c(0.1, 0, 0.9)
kld_result <- kld(p_distribution, q_distribution)
print(kld_result)

}
\seealso{
\code{\link{sum}}, \code{\link{log}}
}
